# Backend API URL (Points to local server in development, configure Vercel URL in production)
VITE_API_BASE_URL=http://localhost:5000/api

# EmailJS Fallback Keys (Optional - only used if not set in MongoDB)
VITE_SERVICE_KEY=
VITE_TEMPLATE_KEY=
VITE_PUBLIC_KEY=
